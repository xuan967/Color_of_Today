# 沉浸式相机页面与纹理 Cover 设计

## 目标

相机取景画面铺满整个窗口并延伸到系统状态栏、底部导航区域之下；页面控件全部作为覆盖层。顶部显示今日颜色和节气文字，底部使用一整块不透明黑色控制区容纳缩略图、快门与镜头切换。

## 已确认布局

```text
┌──────────────────────────┐
│ 系统状态栏 / 刘海安全区   │ ← 相机画面位于其下
│      ● 青瓷绿  ›          │
│         华灯初上          │
│                          │
│        全屏取景画面       │
│                          │
│                          │
├──────────────────────────┤
│  缩略图      快门      切换 │ ← 不透明黑色浮层
│       底部手势安全区       │
└──────────────────────────┘
```

底部黑色区域覆盖在相机 Surface 上方，但不参与 Surface 的布局和尺寸计算。相机仍保持完整窗口大小，因此控制层的高度不会产生新的预览黑带或改变 Camera Profile 选择。

## 第一性原理

全屏取景需要分别满足两个条件：

1. **显示几何铺满窗口。** ArkUI 根节点和 XComponent 必须覆盖完整窗口，系统栏改为透明叠加；交互控件根据安全区避让刘海和底部手势条。
2. **纹理有效内容铺满 Surface。** NativeImage 的完整 Buffer Matrix 是生产端提供的旋转、平移和有效内容区真值。Shader 必须使用完整矩阵，再按 Surface 与有效画面的宽高比执行等比 cover。不能丢弃矩阵平移，也不能用纹理分配尺寸猜测内容区域。

## Native 渲染设计

- 使用 `OH_NativeImage_GetBufferMatrix` 获取包含旋转和有效内容区的完整 4×4 矩阵。
- Shader 使用完整矩阵变换 UV，不再只取 2×2 线性部分并手工补中心平移。
- 删除/停用 `glGetTexLevelParameteriv` 推算 pad 的路径，避免旋转后把 padding 应用到错误轴。
- 从矩阵映射四个 UV 角，计算有效采样矩形的宽高比；结合相机 buffer 尺寸与旋转方向计算 cover。
- 保留上一轮已经实现的帧通知驱动消费和最后有效矩阵缓存。
- cover 只做等比居中裁切，绝不进行非等比拉伸。

## ArkUI 布局设计

- EntryAbility 开启沉浸式窗口布局，并让状态栏、导航栏背景透明。
- XComponent 是根 Stack 的第一个子节点，宽高均为 `100%`。
- 顶部 HUD 使用安全区顶部 inset 加固定视觉间距定位；颜色胶囊可点击，下方字段居中显示。
- 九宫格、对焦环、加载/错误态继续覆盖在预览上。
- 底部控制面板为不透明黑色矩形，固定在窗口底部；其内容底部 padding 包含导航/手势安全距离。
- 控制面板高度保持稳定，不因文字、计数或镜头能力变化改变 XComponent 尺寸。

## 风险与约束

- cover 会裁掉少量横向或纵向边缘，这是保持比例并铺满屏幕的必然代价。
- 系统栏透明后必须使用安全区，避免顶部文字与刘海重叠、底部按钮进入手势区。
- 模拟器和真机可能提供不同 Buffer Matrix；必须记录矩阵、Surface、buffer 和 cover 参数，并在至少一种真机上复核。
- 拍摄截图使用与预览相同的全屏 framebuffer，因此成片构图与预览保持一致；底部控制面板不进入拍摄截图。

## 验证标准

- 状态栏下方不再出现预览黑带，取景画面从窗口顶部延伸至底部黑色控制面板之后。
- “青瓷绿”和下方字段位于页面顶部安全区。
- 底部三个主控件完整位于一整块黑色区域中。
- 人脸、圆形和方格不发生纵向或横向拉伸。
- 前后镜头切换后仍保持全屏 cover 和正确镜像。
- ArkTS、arm64-v8a、x86_64 编译及 HAP 打包通过。
